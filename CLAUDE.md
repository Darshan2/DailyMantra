# CLAUDE.md

## Android tooling

For Android SDK management, AVD/emulator management, device interaction, or Android CLI documentation lookups, always use the `android-cli` skill rather than invoking `adb`/`sdkmanager`/`avdmanager` directly via Bash.
